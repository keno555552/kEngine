# Copilot Instructions

## 專案指導方針
- User is writing a custom C++ game engine ("kEngine") with a hierarchical particle system using CPU-based emission and instance rendering. Use explicit types like Vector3 and Vector4.