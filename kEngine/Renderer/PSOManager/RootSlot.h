#pragma once

/// t0:TransformMatrices	(VS)
/// b1:InstanceOffset		(VS)
/// t1:Well(skeletonSpace)	(VS)

/// b1:Camera				(PS)
/// b2:LightingCount		(PS)
/// t0:Texture				(PS)
/// t1:LightList			(PS)

enum class RootSlot{





};

